<mvc:View xmlns:mvc="sap.ui.core.mvc"
          controllerName="project1.controller.JsonModel"
          xmlns="sap.m"
          xmlns:l="sap.ui.layout">
          <List items="{empModel>/employees}">
    <StandardListItem title="{empModel>name}" description="{empModel>city}"/>
    </List>
</mvc:View>